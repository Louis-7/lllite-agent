# lllite-agent
A light weight agent for self use

## Development

Requires Node.js 22.6 or newer. Node runs the TypeScript source directly with
native type stripping; no JavaScript build step is needed.

```bash
npm install
npm start
```

Use `npm run dev` for watch mode and `npm run typecheck` for strict type checking.
